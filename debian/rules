#!/usr/bin/make -f
# debian/rules
# -*- makefile -*-

export DESTROOT=$(CURDIR)/debian/penguintuner

%:
	dh $@

override_dh_clean:
	rm -rf obj-*
	dh_clean
	dh_auto_clean

#override_dh_build:
#ifeq "${HOST_ARCH}" ""
#	./build.sh
#else
#	./${HOST_ARCH}-build.sh
#endif

override_dh_auto_install:
	dh_auto_install
	cd obj* && meson install
