[constants]
arch = 'arm-linux-gnueabihf'
[binaries]
c = arch + '-gcc'
strip = arch + '-strip'
pkgconfig = arch + '-pkg-config'
[properties]
host_arch = 'armhf'

[host_machine]
system = 'linux'
cpu_family = 'arm'
cpu = 'armhf'
endian = 'little'

